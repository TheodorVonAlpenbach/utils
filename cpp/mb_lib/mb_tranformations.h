#ifndef _MB_TRANSFORMATIONS_H
#define _MB_TRANSFORMATIONS_H

#include <list>
#include "mb_functional_spec.h"
#include <algorithm>

namespace mb {
//    template<class T, class Key, class Value>
//    vector<T> map2vector(const map<Key,Value>& m, 
//  		       const unary_function<pair<const Key, Value>, T>& op) {
//      vector<T> ret(m.size());
//      std::transform(m.begin(), m.end(), ret.begin(), op);
//      return ret;
//    }
}

#endif //_MB_TRANSFORMATIONS_H
